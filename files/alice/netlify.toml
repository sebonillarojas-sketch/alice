[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  # @supabase/supabase-js@2.110+ exige Node >=22; con Node 20 el build de Netlify falla.
  NODE_VERSION = "22"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
