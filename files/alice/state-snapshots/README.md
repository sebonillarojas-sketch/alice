# Snapshots de estado local (hygge:*)

Para mover el trabajo entre máquinas mientras el sync de `app_state` en Supabase
siga roto (RLS). Exportar/importar desde la Mesa de Trabajo: botón "⬇ Estado"
descarga el .json; botón "⬆" lo carga y recarga la app.
